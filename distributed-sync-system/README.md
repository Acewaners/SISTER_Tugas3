# Distributed Sync System

A distributed synchronization system implementing Raft consensus, distributed locking, message queues, and caching.

## Installation

```bash
pip install -r requirements.txt
```

## Configuration

Copy `.env.example` to `.env` and configure as needed.

## Running

```bash
# Start the system
python -m src.nodes
```